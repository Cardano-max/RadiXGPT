name: quiz_app
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.0.6

  cupertino_icons: ^1.0.2

  flutter_svg: ^0.21.0+1

  get: ^4.3.8

  webview_flutter: ^3.0.0 # Add this line to include the webview_flutter package

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
